# OpenAI API Key (optional - app will use fallback images if not set)
OPENAI_API_KEY=your_openai_api_key_here

# Flask Secret Key (optional - defaults to "dev" in development)
# SECRET_KEY=your_secret_key_here
